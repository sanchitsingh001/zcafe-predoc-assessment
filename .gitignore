# Virtualenv
.venv/

# OS / editor
.DS_Store
.cursor/

# Python / Jupyter
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Census + PLACES at repo root (optional local duplicates; notebook uses raw_data/ only)
/B08201 HOUSEHOLD SIZE BY VEHICLES AVAILABLE/
/B11003FAMILY TYPE BY PRESENCE AND AGE OF OWN CHILDREN UNDER 18 YEARS/
/B25003-Tenure/
/B25014TENURE BY OCCUPANTS PER ROOM/
/S1501-Educational Attainment/
/S1701-Poverty/
/S2301EMPLOYMENT/
/PLACES__Census_Tract_Data_*.csv

# raw_data: keep folder scaffold in git, but not the large raw datasets
# (GitHub hard-limit is 100MB per file; use raw_data.zip via Releases instead)
raw_data/*.csv
raw_data/**/*.csv
raw_data/**/*.txt

# Raw data bundle (upload as a Release asset, not committed)
raw_data.zip
